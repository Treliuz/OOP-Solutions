# Ignore everything
*

# Allow C++ source and header files
!*.cpp
!*.h

# Optional: Allow directories (necessary if ignoring everything with `*`)
!*/

# Ignore build directories and common compiled files
build/
*.o
*.exe
*.out
*.a
*.lib
*.dll
*.so
